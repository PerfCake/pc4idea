package org.perfcake.pc4idea.api.editor.actions;

/**
 * Action types
 *
 * @author Stanislav Kaleta
 */
public enum ActionType {
    UNDO, REDO, COMMIT, EDIT, ADDP, DEL, ADDM, ADDH,
    ATTV, ADDV, TOGGLE, REORDER, ADDR, ADDD, ADDPE
}
